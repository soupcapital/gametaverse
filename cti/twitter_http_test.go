package cti

import (
	"testing"
)

func TestSearch(t *testing.T) {
	// token := NewToken()
	// err := token.Refresh()
	// assert.Equal(t, err, nil)

	// hc := NewTwitterSearchConn()

	// hc.Init(token.token)
	//hc.QueryV("SBF_Alameda")
}
